{
  "name": "duke-realty",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --emptyOutDir",
    "preview": "vite preview --host 0.0.0.0",
    "deploy:check": "npm run build"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
